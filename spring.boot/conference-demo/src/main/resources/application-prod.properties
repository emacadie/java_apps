# spring.datasource.url=jdbc:postgresql://localhost:5433/conference_app
spring.datasource.username=conf_user
spring.datasource.password=this-is-conference

# move the properties to something external
spring.datasource.url=${DB_URL}
# spring.datasource.username=conf_user
# spring.datasource.password=this-is-conference

# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL94Dialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.hibernate.show-sql=true

# this allows us to use binary data
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

server.port=5000
logging.level.org=WARN