package info.shelfunit.gui;
/*
import javafx.application.Application;
import javafx.scene.Scene;
import javafx.scene.control.Label;
import javafx.scene.text.Font;
import javafx.stage.Stage;

public class FirstJFXApp extends Application {
    // extend javafx.application.Application and override start
    public void start( Stage stage ) {
        Label aLabel = new Label( "Hello world" );
        aLabel.setFont( new Font( 100 ) );
        stage.setScene( new Scene( aLabel ) );
        stage.setTitle( "Hello" );
        stage.show();
    } // start

    public static void main( String... args ) {
        // call "launch" in main to actually run it
        launch();
    }
}
*/